name: mysql_dart
description: Native MySQL client written in Dart. Tested with MySQL Percona Server (5.7, 8), MySQL Community Server (9, 9.7), MariaDB (10). Supports TLS and MySQL 9 caching_sha2_password authentication.
version: 1.3.0
homepage: https://github.com/insinfo/mysql.dart
repository: https://github.com/insinfo/mysql.dart

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: '>=2.16.0 <4.0.0'

dependencies:

dev_dependencies:
  
  lints: ^4.0.0
  test: ^1.25.8
 
